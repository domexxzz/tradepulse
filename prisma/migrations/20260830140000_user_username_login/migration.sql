-- เพิ่มชื่อผู้ใช้สำหรับล็อกอิน เพื่อให้เข้าหลังบ้านได้โดยไม่ต้องพิมพ์อีเมล
--
-- เป็น nullable เพราะสมาชิกเดิมทุกคนยังไม่มีค่านี้ และคนที่สมัครผ่านหน้าเว็บ
-- ก็ยังใช้อีเมลล็อกอินได้เหมือนเดิม ไม่ต้องบังคับให้ทุกคนตั้งชื่อผู้ใช้
--
-- unique index บน Postgres ยอมให้มี NULL ซ้ำกันได้หลายแถว จึงไม่ชนกับข้อมูลเดิม
-- ค่าถูกเก็บเป็นตัวพิมพ์เล็กเสมอจากฝั่งแอป (unique ของ Postgres แยกตัวพิมพ์ใหญ่เล็ก
-- ถ้าไม่บังคับ จะมีทั้ง 'Somchai' และ 'somchai' อยู่พร้อมกันแล้วล็อกอินสับสน)
ALTER TABLE "User" ADD COLUMN "username" TEXT;

CREATE UNIQUE INDEX "User_username_key" ON "User"("username");
